name: Fraxlend V1
on:
  pull_request:
    paths:
      - ".github/workflows/fraxlend-v1.yml"
      - "contracts/strategies/Strategy.sol"
      - "contracts/strategies/fraxlend/*.sol"
      - "contracts/strategies/fraxlend/v1/*.sol"
      - "test/Strategy*.sol"
      - "test/fraxlend/v1/*.sol"

jobs:
  check:
    name: Foundry project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Use nvmrc
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"

      - name: Run npm install
        run: npm i

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable

      - name: Run tests
        run: forge t --mc FraxlendV1 -vvv
        env:
          ETHEREUM_FORK_NODE_URL: ${{ secrets.ETHEREUM_FORK_NODE_URL }}
          OPTIMISM_FORK_NODE_URL: ${{ secrets.OPTIMISM_FORK_NODE_URL }}
          BASE_FORK_NODE_URL: ${{ secrets.BASE_FORK_NODE_URL }}
